<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /teacher/
    RewriteRule ^index\.html$ - [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /teacher/index.html [L]
</IfModule>
